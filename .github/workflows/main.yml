name: Deploy Pygame to GitHub Pages

on:
  push:
    branches: [ main ]  # Runs every time you push to main

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install pygbag==0.8.4 

      - name: Build and Organize
        run: |
          mkdir -p deploy
          cp index.html ads.txt privacy.html terms.html about.html contact.html deploy/ 2>/dev/null || true
          
          for dir in */; do
            dirname=${dir%/}
            if [ -f "$dirname/main.py" ]; then
              echo "Building: $dirname"
              python -m pygbag --build "$dirname/"
              
              mkdir -p "deploy/$dirname"
              # Sadece pygbag'in ürettiği klasörü kopyala, manuel müdahale etme
              cp -r "$dirname/build/web/"* "deploy/$dirname/"
              
              # Sadece senin özel HTML dosyanı dışarıdan ekle
              cp "$dirname/blockdodge_play.html" "deploy/$dirname/" 2>/dev/null || true
            fi
          done

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: deploy
          branch: gh-pages

          
